category: data_validation
cases:
- id: password-reset-email-data-status-json
  prompt: 'For the "password reset email" feature, return its status as JSON: {"ok": <boolean>, "code":
    <integer>}.'
  validator: json_schema
  args:
    properties:
      ok: boolean
      code: integer
